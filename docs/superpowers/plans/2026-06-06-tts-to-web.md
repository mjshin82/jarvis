# TTS 출력을 웹 클라이언트로(presence) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** owner 웹 뷰어가 1개 이상이면 TTS 를 항상 웹(모든 owner 클라이언트)으로, 없으면 로컬 스피커로.

**Architecture:** DO 가 viewer 접속/해제마다 publisher 에게 owner 뷰어 수(`{kind:"viewers",count}`)를 통지. RelayClient 가 수신해 `web_viewer_count` 보관. speak_response 가 그 값으로 웹/로컬 라우팅(웹 전송은 broadcastBinary 가 전 owner 에 자동 fan-out).

**Tech Stack:** Python(asyncio, websockets, pytest) · Cloudflare Worker(TS, DO).

전제: `cd /Users/oracle/Documents/concode/jarvis`. pytest: `.venv/bin/python -m pytest`. typecheck: `cd jarvis-web && npm run typecheck`.

---

## Task 1: relay — viewers 통지 (types + DO)

**Files:** Modify `jarvis-web/src/types.ts`, `jarvis-web/src/meeting_do.ts`

- [ ] **Step 1: types — viewers kind + count** — `types.ts` 의 EventKind 끝:
```ts
  | "user"
  | "assistant"
  | "navigate";
```
교체:
```ts
  | "user"
  | "assistant"
  | "navigate"
  | "viewers";          // worker → publisher: owner 뷰어 수 통지
```
그리고 `ClientMessage` 인터페이스 — 현재:
```ts
export interface ClientMessage {
  kind: EventKind;
  text?: string;
  meta?: MeetingMeta;
  reason?: string;
  source?: "system" | "remote";
}
```
교체(`count?` 추가):
```ts
export interface ClientMessage {
  kind: EventKind;
  text?: string;
  meta?: MeetingMeta;
  reason?: string;
  source?: "system" | "remote";
  count?: number;
}
```

- [ ] **Step 2: DO — notifyViewerCount 헬퍼** — `meeting_do.ts` 의 `attachViewer` 메서드 **앞**(`// --- viewer ---` 주석 근처, attachViewer 정의 바로 위)에 추가:
```ts
  // owner 뷰어 수를 publisher(jarvis)에게 통지 → TTS 웹/로컬 라우팅 결정에 사용
  private notifyViewerCount(): void {
    if (!this.publisher) return;
    let count = 0;
    for (const role of this.viewers.values()) if (role === "owner") count += 1;
    this.safeSend(this.publisher, this.buildEvent({ kind: "viewers", count }));
  }
```

- [ ] **Step 3: DO — attachViewer 통지** — 현재:
```ts
  private attachViewer(ws: WebSocket, role: "owner" | "public"): void {
    this.viewers.set(ws, role);
    // replay 는 owner 만 (public = 라이브만, 과거 자막 미노출)
    if (role === "owner") {
      for (const ev of this.events) {
        this.safeSend(ws, ev);
      }
      if (this.lastMicSource) {
        this.safeSend(ws, this.buildEvent({ kind: "mic_source", source: this.lastMicSource as "system" | "remote" }));
      }
    }
    ws.addEventListener("close", () => {
      this.viewers.delete(ws);
    });
    ws.addEventListener("error", () => {
      this.viewers.delete(ws);
    });
  }
```
교체(set/delete 후 notify):
```ts
  private attachViewer(ws: WebSocket, role: "owner" | "public"): void {
    this.viewers.set(ws, role);
    this.notifyViewerCount();
    // replay 는 owner 만 (public = 라이브만, 과거 자막 미노출)
    if (role === "owner") {
      for (const ev of this.events) {
        this.safeSend(ws, ev);
      }
      if (this.lastMicSource) {
        this.safeSend(ws, this.buildEvent({ kind: "mic_source", source: this.lastMicSource as "system" | "remote" }));
      }
    }
    ws.addEventListener("close", () => {
      this.viewers.delete(ws);
      this.notifyViewerCount();
    });
    ws.addEventListener("error", () => {
      this.viewers.delete(ws);
      this.notifyViewerCount();
    });
  }
```

- [ ] **Step 4: DO — attachPublisher 통지** — 현재:
```ts
    this.publisher = ws;

    ws.addEventListener("message", (msg) => {
```
교체(publisher 등록 직후 현재 수 통지):
```ts
    this.publisher = ws;
    this.notifyViewerCount();   // 재연결한 publisher 에게 현재 owner 수 동기화

    ws.addEventListener("message", (msg) => {
```

- [ ] **Step 5: 타입체크 + 커밋**

Run: `cd jarvis-web && npm run typecheck` → 오류 없음
Run: `cd /Users/oracle/Documents/concode/jarvis && grep -c 'notifyViewerCount\|"viewers"' jarvis-web/src/meeting_do.ts jarvis-web/src/types.ts` → 합계 5 이상
```bash
git add jarvis-web/src/types.ts jarvis-web/src/meeting_do.ts
git commit -m "feat(DO): owner 뷰어 수를 publisher 에 통지({kind:viewers})"
```

---

## Task 2: relay_client.py — 수신 루프 + web_viewer_count

**Files:** Modify `relay_client.py`, Create `tests/test_relay_client.py`

- [ ] **Step 1: 실패 테스트** — `tests/test_relay_client.py`:
```python
# tests/test_relay_client.py
from relay_client import RelayClient


class _Meta:
    key = "k"


def _rc():
    return RelayClient("ws://x", "tok", _Meta(), on_log=lambda *a: None)


def test_handle_inbound_updates_count():
    rc = _rc()
    assert rc.web_viewer_count == 0
    rc._handle_inbound('{"kind":"viewers","count":3}')
    assert rc.web_viewer_count == 3
    rc._handle_inbound('{"kind":"viewers","count":0}')
    assert rc.web_viewer_count == 0


def test_handle_inbound_ignores_others():
    rc = _rc()
    rc._handle_inbound('{"kind":"viewers","count":2}')
    rc._handle_inbound('{"kind":"something"}')   # 무시 — 유지
    rc._handle_inbound("not json")               # 안전
    rc._handle_inbound(b"\x00\x01")              # bytes 무시
    assert rc.web_viewer_count == 2
```

- [ ] **Step 2: 실패 확인** — `.venv/bin/python -m pytest tests/test_relay_client.py -v` → FAIL (web_viewer_count/_handle_inbound 없음).

- [ ] **Step 3: 구현 — 생성자 필드** — `__init__` 의 `self._connected = asyncio.Event()   # 한 번이라도 연결되면 set` 줄 **다음**에 추가:
```python
        self.web_viewer_count = 0   # DO 가 통지한 owner 뷰어 수 (TTS 라우팅용)
```

- [ ] **Step 4: 구현 — _handle_inbound + recv/send 분리** — 현재 `_connect_once`(send-only 루프):
```python
    async def _connect_once(self) -> None:
        url = self._publish_url()
        headers = {"Authorization": f"Bearer {self.token}"}
        async with websockets.connect(
            url, additional_headers=headers, ping_interval=20, ping_timeout=10,
            open_timeout=self.connect_timeout,
        ) as ws:
            self._ws = ws
            # 1) 매 연결마다 hello 재송신 (재연결 시 새 publisher 로 인수됨)
            await ws.send(json.dumps(self._hello_payload(), ensure_ascii=False))
            self._connected.set()
            # 2) 큐 소비
            while not self._stop.is_set():
                try:
                    msg = await asyncio.wait_for(self._queue.get(), timeout=1.0)
                except asyncio.TimeoutError:
                    continue
                try:
                    await self._send_item(ws, msg)
                except ConnectionClosed:
                    # 큐에 다시 넣고 바깥 _run 에서 재연결
                    try:
                        self._queue.put_nowait(msg)
                    except asyncio.QueueFull:
                        pass
                    raise
                if not isinstance(msg, (bytes, bytearray)) and msg.get("kind") == "end":
                    # end 송신 후 깔끔 종료
                    return
            # stop signal: 큐에 남은 거 더 보낼 필요 없음
            return
```
교체(send/recv 동시 + 종료 시 count 0):
```python
    def _handle_inbound(self, raw) -> None:
        """DO → publisher 인바운드. 현재는 {kind:"viewers", count} 만 처리."""
        if isinstance(raw, (bytes, bytearray)):
            return
        try:
            m = json.loads(raw)
        except Exception:
            return
        if m.get("kind") == "viewers":
            self.web_viewer_count = int(m.get("count") or 0)

    async def _send_loop(self, ws) -> None:
        while not self._stop.is_set():
            try:
                msg = await asyncio.wait_for(self._queue.get(), timeout=1.0)
            except asyncio.TimeoutError:
                continue
            try:
                await self._send_item(ws, msg)
            except ConnectionClosed:
                try:
                    self._queue.put_nowait(msg)
                except asyncio.QueueFull:
                    pass
                raise
            if not isinstance(msg, (bytes, bytearray)) and msg.get("kind") == "end":
                return   # end 송신 후 깔끔 종료

    async def _recv_loop(self, ws) -> None:
        async for raw in ws:
            self._handle_inbound(raw)

    async def _connect_once(self) -> None:
        url = self._publish_url()
        headers = {"Authorization": f"Bearer {self.token}"}
        async with websockets.connect(
            url, additional_headers=headers, ping_interval=20, ping_timeout=10,
            open_timeout=self.connect_timeout,
        ) as ws:
            self._ws = ws
            # 매 연결마다 hello 재송신 (재연결 시 새 publisher 로 인수됨)
            await ws.send(json.dumps(self._hello_payload(), ensure_ascii=False))
            self._connected.set()
            send = asyncio.create_task(self._send_loop(ws))
            recv = asyncio.create_task(self._recv_loop(ws))
            try:
                done, pending = await asyncio.wait({send, recv}, return_when=asyncio.FIRST_COMPLETED)
            finally:
                for t in (send, recv):
                    if not t.done():
                        t.cancel()
                for t in (send, recv):
                    try:
                        await t
                    except (asyncio.CancelledError, Exception):
                        pass
            self.web_viewer_count = 0   # 끊기면 다음 연결까지 모름 → 보수적으로 0(로컬)
            for t in done:
                if t.cancelled():
                    continue
                exc = t.exception()
                if exc and not isinstance(exc, asyncio.CancelledError):
                    raise exc
```

- [ ] **Step 5: 통과 확인** — `.venv/bin/python -m pytest tests/test_relay_client.py -v` (2 pass), `.venv/bin/python -m pytest tests/ -q` (0 failed), `.venv/bin/python -c "import ast; ast.parse(open('relay_client.py').read()); import relay_client; print('ok')"` → ok.

- [ ] **Step 6: 커밋**
```bash
git add relay_client.py tests/test_relay_client.py
git commit -m "feat: RelayClient 수신 루프 + web_viewer_count(viewers 통지 수신)"
```

---

## Task 3: main.py — speak_response 라우팅

**Files:** Modify `main.py`

- [ ] **Step 1: 라우팅 조건 변경** — 현재(speak_response 안):
```python
                if web_pub is not None and mic.router.active == "remote":
                    pcm16 = (np.clip(wav, -1.0, 1.0) * 32767).astype(np.int16).tobytes()
                    web_pub.emit_audio(pcm16, sr)
                    dur = len(wav) / float(sr)
                    web_speaking_until = max(web_speaking_until, time.monotonic()) + dur
                else:
                    await player.enqueue(wav, sr)
```
교체(조건만 변경):
```python
                if web_pub is not None and web_pub.web_viewer_count > 0:
                    pcm16 = (np.clip(wav, -1.0, 1.0) * 32767).astype(np.int16).tobytes()
                    web_pub.emit_audio(pcm16, sr)
                    dur = len(wav) / float(sr)
                    web_speaking_until = max(web_speaking_until, time.monotonic()) + dur
                else:
                    await player.enqueue(wav, sr)
```

- [ ] **Step 2: 검증**

Run: `.venv/bin/python -c "import ast; ast.parse(open('main.py').read()); import main; print('ok')"` → `ok`
Run: `.venv/bin/python -m pytest tests/ -q` → 0 failed
Run: `grep -c 'web_pub.web_viewer_count > 0' main.py` → `1`

- [ ] **Step 3: 커밋**
```bash
git add main.py
git commit -m "feat: TTS 를 웹 뷰어 있으면 웹으로(web_viewer_count>0), 없으면 로컬"
```

---

## Task 4: 통합 검증 + 배포

**Files:** Modify `jarvis-web/scripts/mic_relay_check.mjs`

- [ ] **Step 1: presence 통지 검증 추가** — `mic_relay_check.mjs` 의 `main()`, 최종 정리(`[recv, send, send2, viewer, viewer2].forEach(...)`) **직전**에 삽입:
```javascript
  // 15) viewer presence: subscribe 접속 시 publisher 가 {kind:"viewers", count>=1} 수신
  const presPub = await open(`${BASE}/publish/${KEY}`, { headers: { Authorization: `Bearer ${RELAY}` } });
  const gotV = (async () => {
    for (;;) {
      const m = await nextMsg(presPub);
      if (m.text && /"kind":"viewers"/.test(m.text) && /"count":[1-9]/.test(m.text)) return m.text;
    }
  })();
  const presViewer = await open(`${BASE}/subscribe/${KEY}?token=${ADMIN}`);
  const pvm = await Promise.race([gotV, new Promise((_, r) => setTimeout(() => r(new Error("timeout")), 3000))]).catch((e) => fail(e.message));
  console.log("viewer presence 통지:", pvm.includes('"viewers"') ? "OK" : `FAIL (${pvm})`);
  presViewer.close(); presPub.close();
```

- [ ] **Step 2: node --check** — `node --check jarvis-web/scripts/mic_relay_check.mjs` → SYNTAX OK.

- [ ] **Step 3: best-effort 라이브 런** — `cd jarvis-web && npx wrangler dev --port 8787 > /tmp/jw.log 2>&1 &` (10s, "Ready") → `RELAY_TOKEN=devtoken ADMIN_PASSWORD=adminpw node scripts/mic_relay_check.mjs` → 모든 줄 OK 기대(신규 "viewer presence 통지: OK") → `pkill -f "wrangler dev"`. 안 뜨면 스킵+사유.

- [ ] **Step 4: 커밋**
```bash
cd /Users/oracle/Documents/concode/jarvis
git add jarvis-web/scripts/mic_relay_check.mjs
git commit -m "test: viewer presence({kind:viewers}) 통지 검증"
```

- [ ] **Step 5: 배포 (머지 후 컨트롤러 — 서브에이전트 건너뜀)** — `cd jarvis-web && npm run deploy`. 수동 E2E: 폰 홈 열어둔 채 jarvis 가 시스템 마이크로 대화 → TTS 가 폰에서 재생(로컬 X). 폰 홈 닫으면 로컬 스피커. (jarvis 재시작 필요.)

---

## Self-Review 결과

**Spec coverage:**
- viewers kind/count → Task 1 Step 1 ✓
- DO notifyViewerCount + attachViewer/attachPublisher 통지 → Task 1 Step 2·3·4 ✓
- RelayClient web_viewer_count + recv 루프 + _handle_inbound → Task 2 ✓
- speak_response web_viewer_count>0 라우팅 → Task 3 ✓
- 단위(_handle_inbound) + 통합(presence) → Task 2 Step 1, Task 4 ✓
- 비범위(개별 음소거/동시재생) → 미구현 ✓

**Placeholder scan:** 모든 코드 step 완전. 빈칸 없음.

**Type/이름 consistency:** `viewers`/`count` 일치 — DO `buildEvent({kind:"viewers",count})`(ClientMessage.count) ↔ RelayClient `_handle_inbound` `m.get("count")` → `web_viewer_count` ↔ main `web_pub.web_viewer_count > 0`. `notifyViewerCount` DO 내 일관. `_send_loop`/`_recv_loop`/`_handle_inbound`/`_connect_once` RelayClient 일관.

**핵심 위험:** (1) `_connect_once` send-only → send/recv 동시. 기존 큐 재적재(ConnectionClosed)·end-종료·백오프 동작 보존(send_loop 그대로 옮김). (2) 끊김 시 web_viewer_count=0(로컬 폴백 — 보수적). (3) attachPublisher 통지가 publisher 등록 직후라 ws 열림(server.accept 됨). (4) viewers 는 publisher 전용 — broadcast/replay 경로 안 탐(viewer 에 안 감). (5) public 뷰어는 owner 집계 제외 → 오디오 못 받는 뷰어로 잘못 라우팅 안 함.
