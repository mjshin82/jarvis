{
  "name": "jarvis-web",
  "version": "0.1.0",
  "private": true,
  "description": "Jarvis 회의 모드 자막 중계 — Cloudflare Workers + Durable Objects",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "happy-dom": "^15.11.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.8",
    "wrangler": "^4.97.0",
    "ws": "^8.21.0"
  }
}
