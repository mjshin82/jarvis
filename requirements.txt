# --- Core ---
openai>=1.40.0          # DeepSeek V4 (OpenAI 호환 API)
python-dotenv>=1.0.0    # .env 로드

# --- Audio I/O ---
sounddevice>=0.4.6      # 마이크 캡처 + 스피커 재생
numpy>=1.24.0

# --- VAD (발화 끝 감지) ---
silero-vad>=5.1         # torch 기반 VAD
# torch 는 silero-vad 의존성으로 함께 설치됨 (Apple Silicon: pip 가 알아서 mps 빌드 설치)

# --- Wake word (호출어 'Hey Jarvis') ---
openwakeword>=0.6.0     # 사전학습 hey_jarvis 모델, ONNX 추론. 모델은 첫 실행시 자동 다운로드

# --- STT (Moonshine 한국어, 로컬) ---
useful-moonshine-onnx   # Moonshine ONNX 추론 엔진(MoonshineOnnxModel)
onnxruntime>=1.17.0
huggingface_hub         # 한국어 모델/토크나이저 다운로드 (보통 위 패키지가 함께 설치)
tokenizers              # 한국어 토크나이저 디코딩
# 모델: onnx-community/moonshine-base-ko-ONNX (첫 실행 시 자동 다운로드)

# --- TTS (로컬) ---
supertonic>=1.3.1       # Supertonic 온디바이스 TTS (ONNX). 모델은 첫 실행시 HF 자동 다운로드
soundfile>=0.12.1
