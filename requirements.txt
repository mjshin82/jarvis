# --- Core ---
openai>=1.40.0          # DeepSeek V4 / Ollama (OpenAI 호환 API), 도구 호출
python-dotenv>=1.0.0    # .env 로드
httpx>=0.27.0           # 웹 검색(Serper) 호출 (openai 의존성으로도 설치됨)
yt-dlp>=2025.1.1        # 음악 재생: 유튜브 검색(키 불필요)
prompt_toolkit>=3.0     # 콘솔 하단 고정 입력 프롬프트(로그는 위로 흐름)
num2words>=0.5.14       # TTS 전처리: 숫자 → 한국어/일본어 단어
RealtimeSTT>=1.0        # 회의 모드(/meet) 실시간 자막용. 평상시 STT 와 별도
psutil>=7.0             # CPU/메모리 측정 (PoC 스크립트용)
websockets>=13.0        # 회의 모드 outbound WebSocket publisher (meeting-web 중계)

# --- Audio I/O ---
sounddevice>=0.4.6      # 마이크 캡처 + 스피커 재생
numpy>=1.24.0

# --- VAD (발화 끝 감지) ---
silero-vad>=5.1         # torch 기반 VAD
# torch 는 silero-vad 의존성으로 함께 설치됨 (Apple Silicon: pip 가 알아서 mps 빌드 설치)

# --- Wake word (호출어 'Hey Jarvis') ---
openwakeword>=0.6.0     # 사전학습 hey_jarvis 모델, ONNX 추론. 모델은 첫 실행시 자동 다운로드

# --- STT (faster-whisper, 로컬) ---
faster-whisper>=1.2.0   # CTranslate2 기반 다국어 STT. 모델(large-v3-turbo) 첫 실행시 자동 다운로드
onnxruntime>=1.17.0     # wake/supertonic 에서도 사용

# --- TTS (로컬) ---
supertonic>=1.3.1       # Supertonic 온디바이스 TTS (ONNX). 모델은 첫 실행시 HF 자동 다운로드
soundfile>=0.12.1
